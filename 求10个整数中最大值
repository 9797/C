#include<stdio.h>

int main()
{
	int arr[] = { -6, -9, -8, 5, 7, 3, 1, 0, 107, 2 };
	int max = arr[0];
	int i = 0;
	int sz = sizeof(arr) / sizeof(arr[0]);
	for (i = 1; i < sz; i++)
	{
		if (max < arr[i])
		{
			max = arr[i];
		}
	}
	printf_s("max = %d\n", max);
	return 0;
}
